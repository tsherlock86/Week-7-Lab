<%= bootstrap_form_for(@pin) do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>
  <% end %>

  <div class="field">
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.text_field :image, :value => @pin.image || "http://" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
